pre-commit:
  parallel: true
  commands:
    check:
      glob: "*.{ts,json}"
      run: bun run lint --staged {staged_files}
    typecheck:
      run: bun run typecheck

commit-msg:
  commands:
    commitlint:
      run: bunx commitlint --edit {1}
