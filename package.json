{
  "name": "arb",
  "version": "0.115.4",
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts --",
    "build": "bun run scripts/set-version.ts && bun build src/index.ts --compile --outfile dist/arb && git checkout src/version.ts",
    "test": "ARB_GIT_TIMEOUT=0 bun test src/ --concurrent --timeout 30000",
    "test:integration": "bun run build && bun test test/integration/ --concurrent --timeout 30000",
    "test:integration:git217": "docker build -f test/docker/git217.Dockerfile -t arb-git217 test/docker/ && docker run --rm -v \"$PWD\":/app -w /app arb-git217",
    "test:pbt": "bun run build && bun test test/pbt/ --timeout 300000",
    "test:perf": "bun run build && bun run test/perf/run.ts",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "typecheck": "tsc --noEmit",
    "cycles": "madge --circular --extensions ts src/",
    "check": "bun run lint && bun run typecheck && bun run cycles && bun run test && bun run test:integration",
    "build:release": "bun run scripts/build-release.ts",
    "test:mutate": "bunx stryker run"
  },
  "dependencies": {
    "@inquirer/ansi": "^2.0.4",
    "@inquirer/checkbox": "^5.1.2",
    "@inquirer/confirm": "^6.0.10",
    "@inquirer/core": "^11.1.7",
    "@inquirer/figures": "^2.0.4",
    "@inquirer/input": "^5.0.10",
    "@inquirer/select": "^5.1.2",
    "commander": "^14",
    "liquidjs": "^10.25.2",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4",
    "@commitlint/cli": "^20.5.0",
    "@commitlint/config-conventional": "^20.5.0",
    "@stryker-mutator/core": "^9.6.0",
    "@stryker-mutator/typescript-checker": "^9.6.0",
    "@types/bun": "latest",
    "ajv": "^8.18.0",
    "fast-check": "^4.6.0",
    "madge": "^8.0.0",
    "typescript": "^6.0.2"
  }
}
